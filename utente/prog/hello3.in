#include <sys.h>
#include <lib.h>

process hello3 body hello_body3(0), 20, LIV_UTENTE;

long int wait3=49999999;
process_body hello_body3(int a)
{
	writeconsole("proc3: inizio cpubound");
	while(--wait3);
	writeconsole("proc3: fine");
}

