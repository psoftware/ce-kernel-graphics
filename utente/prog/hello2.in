#include <sys.h>
#include <lib.h>

process hello2 body hello_body2(0), 21, LIV_UTENTE;

long int wait2=28000000;
process_body hello_body2(int a)
{
	writeconsole("proc2: inizio cpubound");
	while(--wait2);

	writeconsole("proc2: inizio iobound");
	for(int i=0; i<5; i++)
	{
		delay(20);
		wait2=5000000;
		while(--wait2);
		writeconsole("proc2: messaggo");
	}
	wait2=1000;
	while(--wait2);
	writeconsole("proc2: fine");
}

