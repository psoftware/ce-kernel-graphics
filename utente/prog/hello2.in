#include <sys.h>
#include <lib.h>

process hello2 body hello_body2(0), 21, LIV_UTENTE;

long int wait2=89999999;
process_body hello_body2(int a)
{
	writeconsole("Inizio Processo2");
	while(--wait2);
	writeconsole("Fine Processo2");
	delay(50);
	wait2=3000000;
	while(--wait2);
}

