#!/bin/bash

QEMU=${QEMU:-qemu}

DEBUG=
if [ "$1" == -g ]; then
	DEBUG="-s -S"
	shift
fi
	
trap 'stty sane' exit

output="-serial stdio"
if [ "$AUTOCORR" == 1 ]; then
	output=-nographic
fi


"$QEMU" $DEBUG		      \
	-kernel build/sistema \
	-hda .swap	      \
	$output 	      \
	-no-reboot 	      |
util/show_log.pl
	
